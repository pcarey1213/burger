<div class="container">

<div class="row justify-content-md-center m-4">
<h1>Eat-Da-Burger!</h1>
<div class="text-center">
  <img alt="Burger" class="img-thumbnail w-25 " src="/assets/img/burger-drawing.png">
</div>
</div>

<div class="row justify-content-md-center">

<div class="col-md-6">

<h2>Burgers still to be eaten!</h2>

<ul class="list-group">
  {{#each burgers}}
    {{#unless devoured}}
      {{>burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

</div>

<div class="col-md=6">
<h2> Burgers that have been devoured!</h2>

<ul class="list-group">
  {{#each burgers}}
    {{#if devoured}}
      {{>burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

</div>

</div>

<div class="row justify-content-md-center m-2">

<div class="card text-center p-2" style="width: 18rem;">
 
  <form class="create-form">

    <div class="form-group">
      <label for="burger"><h2>Add-da-Burger</h2></label>
      <input type="text" class="form-control" id="burger" name="name" placeholder="Enter burger name">
    </div>

    <button type="submit" class="btn btn-primary">Add Burger</button>
  </form>
</div>

</div>
